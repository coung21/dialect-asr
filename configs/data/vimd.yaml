data_dir: data/ViMD_Dataset/data
sampling_rate: 16000
audio_column: audio
text_column: text
num_proc: null

# Optional smoke-test limits. Null means use the complete split.
max_train_samples: null
max_validation_samples: null
max_test_samples: null
